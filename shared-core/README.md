# Shared Core

This directory is reserved for platform-agnostic schema layouts, transaction validation logic, and shared configurations that span across both the Next.js and Flutter frontends.

Future updates will populate this directory with `.json` schemas, common constants, or WASM bindings that are imported universally.
